# === Groq ===
# Required. Get your API key at https://console.groq.com/keys
GROQ_API_KEY=gsk_your_key_here

# Default model for all Groq calls. See app/clients/groq_client.py -> AVAILABLE_MODELS.
DEFAULT_MODEL=llama-3.3-70b-versatile

# === Whisper (offline transcription) ===
# Model size: tiny | base | small | medium
# "small" is the recommended sweet spot on M3 (~5-10s per 60s chunk).
WHISPER_MODEL=small
WHISPER_DEVICE=cpu
WHISPER_COMPUTE_TYPE=int8

# === Recording ===
# Sample rate in Hz. Whisper expects 16000.
SAMPLE_RATE=16000
# Duration of each audio chunk in seconds.
CHUNK_DURATION_SECONDS=60
# How many chunks between live-notes Groq calls (default 5 = ~5 min).
LIVE_NOTES_INTERVAL_CHUNKS=5

# === Storage ===
# Root directory for course/session data (relative to project root).
COURSES_ROOT=courses

# === Server ===
HOST=127.0.0.1
PORT=8000
